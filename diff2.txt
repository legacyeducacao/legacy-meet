diff --git a/app/rooms/[roomName]/page.tsx b/app/rooms/[roomName]/page.tsx
index 3c26972..a19a5cc 100644
--- a/app/rooms/[roomName]/page.tsx
+++ b/app/rooms/[roomName]/page.tsx
@@ -22,7 +22,9 @@ export default async function Page({
       ? _searchParams.codec
       : 'vp9';
   const hq = _searchParams.hq === 'true' ? true : false;
-  const singlePC = _searchParams.singlePC !== 'false';
+  // Modo cl├íssico (2 PeerConnections) por padr├úo ÔÇö mais compat├¡vel com servidores
+  // LiveKit mais antigos. Use ?singlePC=true para for├ºar o modo single-PC.
+  const singlePC = _searchParams.singlePC === 'true';
 
   return (
     <PageClientImpl
